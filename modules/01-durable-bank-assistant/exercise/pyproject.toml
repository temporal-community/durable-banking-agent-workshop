[project]
name = "durable-bank-assistant"
version = "0.1.0"
description = "A bank assistant Agent running as a Temporal workflow"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "temporalio[openai-agents,opentelemetry]>=1.17.0,<2",
    "httpx>=0.27",
]

[tool.uv]
package = false
