[project]
name = "banking-solution-backend"
version = "0.1.0"
description = "Temporalized banking backend: transfers run as a durable workflow"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "temporalio[openai-agents,opentelemetry]>=1.17.0,<2",
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "httpx>=0.27",
]

[tool.uv]
package = false
